<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace:用于指定当前的映射文件和哪个接口进行映射，需要指定接口的文件路径，需要标注包的完整路径接口-->
<mapper namespace="com.example.internetstore.mapper.DistrictMapper">
<!--自定义映射规则：resultMap-->
<!--    id属性：标签给这个映射负责分配一个唯一的id值，对应的就是rsultMap="id属性的值"属性的取值-->
<!--    type:取值是一个类，表示的是数据库中的拆线呢结果与Java中哪个实体类进行结果集的映射-->

    <!--resultType:表示查询的结果集类型，只需要指定对应映射的类型，并且包含完整包接口-->
    <select id="fingByParent" resultType="com.example.internetstore.Entity.District">
        select * from t_dict_district where parent = #{parent} order by code ASC
    </select>
<!--    id:表示映射接口中方法的名称
        useGenrateKeys:表示开启某个字段的值递增（主键设置为递增）
        keyProperty:标签将表中的哪个字段作为主键进行递增
-->
    <select id="findBycode" resultType="java.lang.String">
        select name from t_dict_district where code = #{code}
    </select>


</mapper>